import express from 'express';

// * Do not delete the below comment if you are using Polp.js to generate features/resources
// * inject feature/resource router imports below

const router = express.Router();

// * Do not delete the below comment if you are using Polp.js to generate features/resources
// * inject feature/resource routers below

export { router as appRouter };
